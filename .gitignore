python-api/
# Wails build artifacts
build/
*.exe
*.dll
*.so
*.dylib
*.test
*.out

# Node.js dependencies (if using a frontend framework that uses npm/yarn)
node_modules/
frontend/package-lock.json
frontend/yarn.lock
frontend/dist/*
!frontend/dist/.gitkeep

# Bundled OCR model files (~30 MB — fetched by frontend/scripts/download-models.mjs,
# which runs automatically on `npm run dev` / `npm run build`, or `npm run models`)
frontend/public/models/

# Go module cache
vendor/

# IDE/OS specific files
.idea/
.vscode/
*.swp
*.bak
.DS_Store
Thumbs.db
